// @sl/engine — orchestration: fixed-timestep GameLoop (T21), top-level XState gameMachine (T32),
// and the Game composition root that wires the subsystem packages (later M1 Phase A tasks).
export { GameLoop } from './GameLoop';
export type { GameLoopOptions } from './GameLoop';
